cd Documents/ (go to any directory)
mkdir mhi-layers (make a folder)
cd mhi-layers/ (go inside the folder)
mkdir python (create folder python)
cd python (go inside that folder)
pip3 install jwt -t . (install the libraries)
ls (check libraries)
cd .. (go one folder back)
zip -r layer.zip . (zip that folder with a name)
ls (check the zip is created or not)
